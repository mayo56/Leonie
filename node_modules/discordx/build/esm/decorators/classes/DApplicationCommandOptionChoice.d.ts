import { Decorator } from "@discordx/internal";
import type { ApplicationCommandOptionChoice } from "discord.js";
/**
 * @category Decorator
 */
export declare class DApplicationCommandOptionChoice extends Decorator {
    private _name;
    private _value;
    get name(): string;
    set name(value: string);
    get value(): string | number;
    set value(value: string | number);
    protected constructor(name: string, value: string | number);
    static create(name: string, value: string | number): DApplicationCommandOptionChoice;
    toJSON(): ApplicationCommandOptionChoice;
}
