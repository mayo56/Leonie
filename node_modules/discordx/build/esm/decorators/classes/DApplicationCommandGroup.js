import { Decorator } from "@discordx/internal";
/**
 * @category Decorator
 */
export class DApplicationCommandGroup extends Decorator {
    name;
    infos;
    constructor(name, infos) {
        super();
        this.name = name;
        this.infos = infos ?? {};
    }
    static create(name, infos) {
        return new DApplicationCommandGroup(name, infos);
    }
}
//# sourceMappingURL=DApplicationCommandGroup.js.map