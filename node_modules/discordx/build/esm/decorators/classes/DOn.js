import { Method } from "./Method.js";
/**
 * @category Decorator
 */
export class DOn extends Method {
    _event;
    _once;
    _botIds;
    get botIds() {
        return this._botIds;
    }
    set botIds(value) {
        this._botIds = value;
    }
    get event() {
        return this._event;
    }
    set event(value) {
        this._event = value;
    }
    get once() {
        return this._once;
    }
    set once(value) {
        this._once = value;
    }
    constructor(event, once, botIds) {
        super();
        this._event = event;
        this._once = once;
        this._botIds = botIds ?? [];
    }
    static create(event, once, botIds) {
        return new DOn(event, once, botIds);
    }
    parseParams() {
        return [];
    }
}
//# sourceMappingURL=DOn.js.map