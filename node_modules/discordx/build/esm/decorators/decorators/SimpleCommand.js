import { DSimpleCommand, MetadataStorage } from "../../index.js";
export function SimpleCommand(name, options) {
    return function (target, key) {
        name = name ?? key;
        const cmd = DSimpleCommand.create(name, options?.aliases, options?.argSplitter, options?.botIds, options?.defaultPermission, options?.description, options?.directMessage, options?.guilds, options?.prefix).decorate(target.constructor, key, target[key]);
        MetadataStorage.instance.addSimpleCommand(cmd);
    };
}
//# sourceMappingURL=SimpleCommand.js.map