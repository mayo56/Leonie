import { ComponentType, DComponent, MetadataStorage } from "../../index.js";
export function SelectMenuComponent(id, options) {
    return function (target, key) {
        const button = DComponent.create(ComponentType.SelectMenu, id ?? key, options?.guilds, options?.botIds).decorate(target.constructor, key, target[key]);
        MetadataStorage.instance.addComponentSelectMenu(button);
    };
}
//# sourceMappingURL=SelectMenuComponent.js.map