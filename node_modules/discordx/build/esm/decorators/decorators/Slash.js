import { DApplicationCommand, MetadataStorage } from "../../index.js";
export function Slash(name, options) {
    return function (target, key) {
        name = name ?? key;
        const applicationCommand = DApplicationCommand.create(name, "CHAT_INPUT", options?.description, options?.defaultPermission, options?.guilds, options?.botIds).decorate(target.constructor, key, target[key]);
        MetadataStorage.instance.addApplicationCommandSlash(applicationCommand);
    };
}
//# sourceMappingURL=Slash.js.map