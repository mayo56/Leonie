import { DApplicationCommand, MetadataStorage } from "../../index.js";
export function ContextMenu(type, name, options) {
    return function (target, key) {
        name = name ?? key;
        const applicationCommand = DApplicationCommand.create(name, type, undefined, options?.defaultPermission, options?.guilds, options?.botIds).decorate(target.constructor, key, target[key]);
        if (type == "MESSAGE") {
            MetadataStorage.instance.addApplicationCommandMessage(applicationCommand);
        }
        else {
            MetadataStorage.instance.addApplicationCommandUser(applicationCommand);
        }
    };
}
//# sourceMappingURL=ContextMenu.js.map