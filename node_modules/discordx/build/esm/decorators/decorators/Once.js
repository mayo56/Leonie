import { DOn, MetadataStorage } from "../../index.js";
export function Once(event, options) {
    return function (target, key, descriptor) {
        const myClass = target;
        const on = DOn.create(event, true, options?.botIds).decorate(myClass.constructor, key, descriptor.value);
        MetadataStorage.instance.addOn(on);
    };
}
//# sourceMappingURL=Once.js.map