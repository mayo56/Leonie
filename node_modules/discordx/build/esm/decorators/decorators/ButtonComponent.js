import { ComponentType, DComponent, MetadataStorage } from "../../index.js";
export function ButtonComponent(id, options) {
    return function (target, key) {
        const button = DComponent.create(ComponentType.Button, id ?? key, options?.guilds, options?.botIds).decorate(target.constructor, key, target[key]);
        MetadataStorage.instance.addComponentButton(button);
    };
}
//# sourceMappingURL=ButtonComponent.js.map