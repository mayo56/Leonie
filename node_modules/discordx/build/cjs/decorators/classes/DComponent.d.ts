import type { ComponentType, IGuild } from "../../index.js";
import { Method } from "./Method.js";
/**
 * @category Decorator
 */
export declare class DComponent extends Method {
    private _type;
    private _id;
    private _guilds;
    private _botIds;
    get type(): ComponentType;
    get botIds(): string[];
    set botIds(value: string[]);
    get id(): string | RegExp;
    set id(value: string | RegExp);
    get guilds(): IGuild[];
    set guilds(value: IGuild[]);
    protected constructor(type: ComponentType, id: string | RegExp, guilds?: IGuild[], botIds?: string[]);
    static create(type: ComponentType, id: string | RegExp, guilds?: IGuild[], botIds?: string[]): DComponent;
    isId(text: string): boolean;
    parseParams(): never[];
}
